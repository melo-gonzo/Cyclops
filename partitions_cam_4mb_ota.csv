# Cyclops dual-OTA layout for the AI-Thinker ESP32-CAM (4MB flash).
# Two ~2.0MB app slots (firmware is ~1.1MB, ~54% of a slot) so OTA fits on 4MB.
# nvs is kept at the SAME offset/size as huge_app.csv so WiFi creds, mDNS hostname,
# and all saved audio/camera settings SURVIVE the one-time USB re-flash to this table.
# SPIFFS is dropped (unused: <FS.h> is only pulled in for the SD File type).
# Name,   Type, SubType, Offset,   Size,     Flags
nvs,      data, nvs,     0x9000,   0x5000,
otadata,  data, ota,     0xe000,   0x2000,
app0,     app,  ota_0,   0x10000,  0x1F0000,
app1,     app,  ota_1,   0x200000, 0x1F0000,
coredump, data, coredump,0x3F0000, 0x10000,
